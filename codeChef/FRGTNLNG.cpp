#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, n;
    cin >> t;
    while (t--)
    {
        int N, K;
        cin >> N >> K;
        string Arr[N];
        for (int i = 0; i < N; i++)
        {
            cin >> Arr[i];
        }

        while (K--)
        {
            int L;
            cin >> L;
            while (L--)
            {
                cin >> n;
                string Brr[n];
                for (int i = 0; i < n; i++)
                {
                    cin >> Brr[i];
                }
            }
        }
        for (int i = 0; i < N; i++)
        {
            for (int j = 0; j < n; j++)
            {
                // if(Arr[i]==Brr[i]){

                // }
            }
        }
    }
}